# Local Oracle Database Free source
ORACLE_DSN=localhost:1521/FREEPDB1
ORACLE_USER=migration_demo
ORACLE_SYSTEM_PASSWORD=replace-with-a-separate-local-system-password
ORACLE_PASSWORD=replace-with-a-local-demo-password

# Azure SQL Database target (Microsoft Entra authentication only)
AZURE_SQL_SERVER=your-server.database.windows.net
AZURE_SQL_DATABASE=oracle-migration-demo
